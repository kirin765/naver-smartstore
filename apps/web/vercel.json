{
  "buildCommand": "cd ../.. && corepack pnpm --filter web build",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --frozen-lockfile=false",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
