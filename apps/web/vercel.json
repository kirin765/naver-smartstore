{
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.8 --activate && pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
