{
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --frozen-lockfile=false",
  "buildCommand": "corepack pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
